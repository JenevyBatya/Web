import { SvgResizeDirective } from './svg-resize.directive';

describe('SvgResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgResizeDirective();
    expect(directive).toBeTruthy();
  });
});
