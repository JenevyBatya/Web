import { DrawFiguresDirective } from './draw-figures.directive';

describe('DrawFiguresDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawFiguresDirective();
    expect(directive).toBeTruthy();
  });
});
